# CryptoS.GraphTheory

Library for working with graph. At the present library supports graph traversal with DFS (using recursion or stack) and BFS. The library also supports finding connected components on undirected graphs.
